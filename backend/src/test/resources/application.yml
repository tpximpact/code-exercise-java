spring:
  profiles:
    active:
      - test


app:
  cache:
    ttl-seconds: 600
  cors:
    allowed-methods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
    allowed-headers:
      - "*"
    allow-credentials: true
