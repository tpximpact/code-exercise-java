services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
  frontend:
    build:
      context: ./FrontEnd
      dockerfile: Dockerfile
    ports: 
      - "3000:3000" 
